name: Update Scoring Leaders

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape-and-render:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate graphics
        run: npm run build

      - name: Commit output files
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update scoring leaders graphics"
          file_pattern: output/top10.json output/NZIHL_top10.png output/goalies.json output/NZIHL_goalies.png output/womens_top10.json output/womens_top10.png output/womens_goalies.json output/womens_goalies.png
